{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Esquema para Catálogo de Tareas de Limpieza",
    "description": "Define una estructura para las tareas de limpieza del sistema.",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "id": {
                "description": "Un identificador único para la tarea.",
                "type": "string"
            },
            "description": {
                "description": "Una descripción clara de lo que hace la tarea.",
                "type": "string"
            },
            "type": {
                "description": "El tipo de tarea a ejecutar.",
                "enum": [
                    "SimpleCommand",
                    "DiskCleanup",
                    "FindLargeFiles",
                    "AnalyzeProcesses",
                    "SetDNS"
                ]
            },
            "details": {
                "description": "Un objeto que contiene los parámetros específicos para la tarea.",
                "type": "object"
            }
        },
        "required": [
            "id",
            "description",
            "type",
            "details"
        ]
    }
}
